import {
    setError,
    hideError,
    setStatus,
    resetStatus,
    namespace,
    errorReducer,
} from './errorSlice';

export {setError, hideError, setStatus, resetStatus, namespace, errorReducer};
